import { BetterAttrDirective } from './better-attr.directive';

describe('BetterAttrDirective', () => {
  it('should create an instance', () => {
    const directive = new BetterAttrDirective();
    expect(directive).toBeTruthy();
  });
});
