import { BasicAttrDirective } from './basic-attr.directive';

describe('BasicAttrDirective', () => {
  it('should create an instance', () => {
    const directive = new BasicAttrDirective();
    expect(directive).toBeTruthy();
  });
});
